"use client ";

import { LucideIcon } from "lucide-react";

interface SideBarItemProps {
  icon: LucideIcon;
  label: string;
  href: string;
}

export const SidebarItem = ({ icon, label, href }: SideBarItemProps) => {
  return <div>SidebarItem!qq</div>;
};
